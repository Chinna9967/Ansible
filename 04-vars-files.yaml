- name: variables from files
  hosts: mongodb #managin the ansible server itself
  var_files:
  - variables.yaml
  tasks:
  - name: printing variables
    ansible.builtin.debug:
      msg: "we are learning {{COURSE}}, Duration is {{DURATION}}, Trainer is: {{TRAINER}}"
